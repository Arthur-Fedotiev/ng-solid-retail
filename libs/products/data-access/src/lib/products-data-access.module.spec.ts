import { ProductsDataAccessModule } from './products-data-access.module';

describe('ProductsDataAccessModule', () => {
  it('should create an instance', () => {
    expect(new ProductsDataAccessModule()).toBeTruthy();
  });
});
